export const APP_NAME = 'Sesaku';

export const STORAGE_KEYS = {
  BUDGET_STORE: 'budget-store',
  THEME: 'theme-preference',
} as const;

export const API_ENDPOINTS = {
  AUTH_ME: '/auth/me',
} as const;
